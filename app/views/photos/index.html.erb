<h1> All Photos </h1>

<div>
    <%= link_to "Add Photo", new_photo_path %>
</div>


  <% @photos.each do |photo| %>
     <p> <%= link_to photo.title, photo_path(photo) %> </p>
     <p> <%= image_tag photo.image.url(:small)%></p>
  <% end %>


