<!DOCTYPE html>
<html>
<head>
  <title>Snap Spot</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= stylesheet_link_tag "gmaps4rails" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Snap Spot</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <% if user_signed_in? %>
                  <li class="divider-vertical"><%= link_to "My Photos", user_path(current_user) %></li>
                  <li class="divider-vertical"><%= link_to "Logout", destroy_user_session_path(), method: "DELETE"  %></li>
              <% else %>
                  <li class="divider-vertical"><%= link_to "Login", new_user_session_path()  %></li>
                  <li class="divider-vertical"><%= link_to "Sign Up", new_user_registration_path() %></li>
              <% end %>
              <li class="divider-vertical"><%= link_to "Upload", new_photo_path() %></li>
             </ul>
             <form class="navbar-search pull-right" method= "GET" action="/search">
                <input id="query" name="query" type="text" class="search-query" placeholder="Search" >
              </form>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class = "container-fluid">
      <div class = "span12">
        <%= yield %>
      </div>
      <p class="span12">&copy; Clique City 2013</p>
    </div>

  <footer class="container-fluid">
    <%= yield :scripts %>
  </footer>

</body>
</html>
